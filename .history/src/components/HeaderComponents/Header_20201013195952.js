import React from "react";

import {
    POSTS,
    COMMENTS
} from "../../constants/urls";

const Header = ({ changeLocation }) => {
    return(
        <div> 


        </div>
    )
}

export default Header;